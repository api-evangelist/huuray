naftiko: 1.0.0-alpha2
info:
  label: Huuray API — Template
  description: 'Huuray API — Template. 1 operations. Lead operation: Used to get a list of the available templates. Self-contained
    Naftiko capability covering one Huuray business surface.'
  tags:
  - Huuray
  - Template
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    HUURAY_API_KEY: HUURAY_API_KEY
capability:
  consumes:
  - type: http
    namespace: huuray-template
    baseUri: ''
    description: Huuray API — Template business capability. Self-contained, no shared references.
    resources:
    - name: v4-Template
      path: /v4/Template
      operations:
      - name: post
        method: POST
        description: Used to get a list of the available templates
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: X-API-NONCE
          in: header
          type: string
          description: A random value that can only be used once every 60 days. (max 50 characters)<br /><small>(this prevents
            your message from being re-transmitted, and thereby also
          required: true
        - name: X-API-HASH
          in: header
          type: string
          description: 'The SHA512 hash of a concatenated string containing the following: ( API-SECRET + NONCE ).<br /><small>(this
            is used to authenticate you)</small>'
          required: true
    authentication:
      type: apikey
      key: X-API-TOKEN
      value: '{{env.HUURAY_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: huuray-template-rest
    port: 8080
    description: REST adapter for Huuray API — Template. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/v4/template
      name: v4-template
      description: REST surface for v4-Template.
      operations:
      - method: POST
        name: post
        description: Used to get a list of the available templates
        call: huuray-template.post
        with:
          X-API-NONCE: rest.X-API-NONCE
          X-API-HASH: rest.X-API-HASH
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: huuray-template-mcp
    port: 9090
    transport: http
    description: MCP adapter for Huuray API — Template. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: used-get-list-available-templates
      description: Used to get a list of the available templates
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: huuray-template.post
      with:
        X-API-NONCE: tools.X-API-NONCE
        X-API-HASH: tools.X-API-HASH
      outputParameters:
      - type: object
        mapping: $.
