naftiko: 1.0.0-alpha2
info:
  label: Huuray API — Catalogue
  description: 'Huuray API — Catalogue. 1 operations. Lead operation: Used to get a list of the available products. Self-contained
    Naftiko capability covering one Huuray business surface.'
  tags:
  - Huuray
  - Catalogue
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    HUURAY_API_KEY: HUURAY_API_KEY
capability:
  consumes:
  - type: http
    namespace: huuray-catalogue
    baseUri: ''
    description: Huuray API — Catalogue business capability. Self-contained, no shared references.
    resources:
    - name: v4-Catalogue
      path: /v4/Catalogue
      operations:
      - name: post
        method: POST
        description: Used to get a list of the available products
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: X-API-NONCE
          in: header
          type: string
          description: A random value that can only be used once every 60 days. (max 50 characters)<br /><small>(this prevents
            your message from being re-transmitted, and thereby also
          required: true
        - name: X-API-HASH
          in: header
          type: string
          description: 'The SHA512 hash of a concatenated string containing the following: ( API-SECRET + NONCE ).<br /><small>(this
            is used to authenticate you)</small>'
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-TOKEN
      value: '{{env.HUURAY_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: huuray-catalogue-rest
    port: 8080
    description: REST adapter for Huuray API — Catalogue. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/v4/catalogue
      name: v4-catalogue
      description: REST surface for v4-Catalogue.
      operations:
      - method: POST
        name: post
        description: Used to get a list of the available products
        call: huuray-catalogue.post
        with:
          X-API-NONCE: rest.X-API-NONCE
          X-API-HASH: rest.X-API-HASH
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: huuray-catalogue-mcp
    port: 9090
    transport: http
    description: MCP adapter for Huuray API — Catalogue. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: used-get-list-available-products
      description: Used to get a list of the available products
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: huuray-catalogue.post
      with:
        X-API-NONCE: tools.X-API-NONCE
        X-API-HASH: tools.X-API-HASH
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
